// Platform-resolved by Metro bundler (.native.tsx / .web.tsx).
// This stub lets TypeScript resolve the module — actual implementation
// is in stripe.native.tsx (React Native) and stripe.web.tsx (Expo Web).
export { StripeProvider, useStripe } from "./stripe.native";
