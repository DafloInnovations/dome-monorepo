# Keep react/react-dom resolved from the workspace pnpm store so all
# Next.js SSR renders share a single React dispatcher instance.
# Without this, pnpm installs a separate local copy that diverges from
# the copy react-dom uses, causing useContext to fail during prerendering.
hoist-pattern[]=*
shamefully-hoist=false
